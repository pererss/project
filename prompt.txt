Ты — Lead Frontend & UI/UX Developer.
Задача: полностью переработать веб-мессенджер "SentCor" под премиальный ЧЁРНО-БЕЛЫЙ ГРАДИЕНТНЫЙ СТИЛЬ (Monochrome Dark Luxury), исправить все обработчики кнопок и добавить саб-панели переключения.

Рабочая директория: C:\Users\petr2\Desktop\project

===============================================================
1. ИСПРАВЛЕНИЕ ОШИБОК И РАБОТОСПОСОБНОСТЬ (КРИТИЧЕСКИ ВАЖНО)
===============================================================
1. Запросы к profiles:
   - При запросах к `profiles` запрашивай: `id`, `username`, `avatar_url`, `status`, `email`, `bio`.
   - Везде используй `try..catch`, чтобы при отсутствии какого-то поля интерфейс НЕ ломался и кнопки продолжали работать.

2. Рабочие обработчики событий (Event Listeners):
   - Убедись, что абсолютно КАЖДАЯ кнопка ("Чаты", "Друзья", "Добавить", "Настройки", "Принять заявку", "Удалить", "Отправить") имеет чёткий click-listener в `app.js` или `ui.js`.
   - Кнопка "Написать сообщение" в списке друзей должна мгновенно переключать на вкладку чата с этим пользователем.

===============================================================
2. НОВЫЙ ДИЗАЙН: ЧЁРНО-БЕЛЫЙ ГРАДИЕНТ И SVG-ИКОНКИ
===============================================================
- Цветовая палитра:
  * Основной фон: Глубокий тёмный с плавными градиентами (`linear-gradient(135deg, #09090B 0%, #18181B 100%)`).
  * Панели и карточки: Тёмно-серые плашки с размытием и лёгким белым свечением (`background: rgba(255, 255, 255, 0.03)`, `border: 1px solid rgba(255, 255, 255, 0.08)`, `backdrop-filter: blur(12px)`).
  * Акценты и кнопки: Монохромные градиенты от чисто белого к серебряному (`linear-gradient(135deg, #FFFFFF 0%, #A1A1AA 100%)`, цвет текста тёмный `#09090B`, hover: ярко-белое свечение `box-shadow: 0 0 15px rgba(255,255,255,0.3)`).
  * Текст: Основной — `#FAFAFA`, второстепенный — `#A1A1AA`.

- Аватарки и эмодзи:
  * Все аватарки пользователей строго обрезаются до пропорции 1:1, размер ограничен (в списках 40x40px, в модальных окнах 80x80px, максимум до 150x150px) через `object-fit: cover` и `border-radius: 50%` или `12px` с тонкой белой рамкой.
  * Вместо стандартных системных эмодзи используй стильные SVG-иконки (Lucide / Heroicons стиль в виде чистых SVG) для кнопок, микрофона, шестеренки, поиска и сообщений.

===============================================================
3. СТРУКТУРА САБ-ПАНЕЛЕЙ И ФУНКЦИОНАЛ
===============================================================

А. Левая главная рельса (Server Rail / App Switcher, width 72px):
   - Иконка логотипа SentCor с белым градиентным свечением.
   - Иконка ЛС / Чатов.
   - Разделитель.
   - Иконка "+" (Создать сервер / чат).

Б. Вторая панель (Саб-панель навигации, width 260px):
   - Верхние вкладки-переключатели:
     * 💬 **Чаты** (Список активных диалогов с последним сообщением)
     * 👥 **Друзья** (Переход в меню управления друзьями)
     * 🔔 **Заявки** (Входящие и исходящие запросы со счетчиком)
   - Поле поиска с SVG-иконкой лупы.
   - Внизу: Панель текущего пользователя (`User Bar`):
     * Аватар с индикатором статуса (онлайн/офлайн).
     * @username (Никнейм) жирным градиентным шрифтом (БЕЗ email!).
     * Кнопка-шестеренка ⚙️ (Настройки профиля) и Кнопка Выхода.

В. Центральная зона (Чат / Друзья):
   - Раздел "Друзья": Вкладки "Все", "В сети", "Ожидание", "+ Добавить в друзья" (градиентная выделяющаяся кнопка).
   - Раздел "Чат": Шапка с аватаром, статусом и ником. История сообщений с плавным скроллом, красивыми бабблами и блоком ввода текста в стиле "Glassmorphism".

Г. Окно "Настройки" (Profile Modal):
   - Редактирование ника, био (`bio`), статуса ("В сети", "Не беспокоить", "Не активен") и аватара.

ПЕРЕЗАПИШИ ВСЕ ФАЙЛЫ В C:\Users\petr2\Desktop\project ЦЕЛИКОМ:
1. index.html
2. style.css
3. js/config.js
4. js/ui.js
5. js/auth.js
6. js/chat.js
7. js/friends.js
8. js/app.js
также верни сообщения не в слева и как было с разных сторон
